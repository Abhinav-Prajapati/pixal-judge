# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=image_clustering

# Storage Configuration
# Path where images and thumbnails will be stored
# For Docker: This is mounted as a volume to /app/data inside the container
# For local dev: Use an absolute path on your machine (e.g., /home/username/pixsort-data)
STORAGE_ROOT=/home/your_username/pixsort-data

# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
