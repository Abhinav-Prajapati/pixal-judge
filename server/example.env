# .env file for a Python/Docker project

# ---------------------------------------------
# Docker Compose environment variables
# These variables are used by your docker-compose.yml file.
# ---------------------------------------------
POSTGRES_USER=cluster_user
POSTGRES_PASSWORD=secure_password
POSTGRES_DB=image_db

# ---------------------------------------------
# Python application environment variables
# These are used by your application's config.py file.
# ---------------------------------------------
DB_HOST=db
DB_PORT=5432
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}

# ---------------------------------------------
# Other variables (optional)
# ---------------------------------------------
# Add any other environment variables your app needs here.
# For example, a setting for your machine learning model:
# MODEL_VERSION=v1.0